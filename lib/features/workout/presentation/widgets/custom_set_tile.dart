import 'package:flutter/material.dart';

class CustomSetTile extends StatelessWidget {
  const CustomSetTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const DecoratedBox(
      decoration: BoxDecoration(),
      child: Row(
        children: [],
      ),
    );
  }
}
